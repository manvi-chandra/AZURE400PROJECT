trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

stages:
- stage: BuildStage
  displayName: 'Build and Test Application'
  jobs:
  - job: BuildJob
    displayName: 'Build Job'
    steps:
    - task: NodeTool@0
      inputs:
        versionSpec: '20.x'
      displayName: 'Install Node.js'

    - script: |
        cd helloworld
        npm install
      displayName: 'Install dependencies'

    - script: |
        cd helloworld
        npm test
      displayName: 'Run Tests (Intentional Failure)'